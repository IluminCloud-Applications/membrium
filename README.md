# MEMBRIUM

Área de Membros para cursos online.

## Push (após o inicial)

```bash
git add .
git add -A
git commit -m "v1.1 - chatwoot sync"
git push
```


## Push Inicial

```bash
git add -A
git commit -m "v1.0"
git branch -M main
git push -u origin main
```

